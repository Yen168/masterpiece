require 'spec_helper'

describe Masterpiece do
  it 'has a version number' do
    expect(Masterpiece::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
